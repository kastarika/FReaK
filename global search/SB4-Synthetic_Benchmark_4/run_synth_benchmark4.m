function [tout, yout] = run_synth_benchmark4(~, u, T)
    assignin('base','u',u);
    assignin('base','T',T);
    
    result = sim('phi4_m2_vr001_k5_3', ...
        'StopTime', 'T', ...
        'LoadExternalInput', 'on', 'ExternalInput', 'u', ...
        'SaveTime', 'on', 'TimeSaveName', 'tout', ...
        'SaveOutput', 'on', 'OutputSaveName', 'yout', ...
        'SaveFormat', 'Array');
    tout = result.tout;
    yout = result.yout;
end
